sudo: false
language: python
python: 3.8

branches:
 only:
   - master

install:
    - pip install -e .[test]

# Each step below must succeed for the build to pass
script:
    - python3-coverage run --source=. test.py
    - python3-coverage report -m
    - pep8 --format=pylint --count .
